name: trial_workflow   #name of worklow

on:                     # event trigger
  push:
    branches: [main]


jobs:
  build:                       #jobID
    runs-on: ubuntu-latest    #runner for the job

    steps:
      - name: checkout code
        uses: actions/checkout@v4
    
    


 

  
